# Normalize index.html to root /
DirectoryIndex index.html

# Enable URL rewriting
RewriteEngine On

# Redirect /index.html to root /
RewriteRule ^index\.html$ /travel-booking/ [R=301,L]

# API routes
RewriteRule ^api/(.*)$ api/index.php [L,QSA]